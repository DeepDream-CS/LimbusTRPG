# Limbus Company TRPG 工具集

纯前端、无构建、无依赖。三个页面共用一份 CSS 与 localStorage。中文注释、中文文件名。

| 文件 | 行数 | 职责 |
|---|---|---|
| `index.html` + `建卡器.js` | 3136 | 8 步车卡向导。**全部规则数据的唯一来源** |
| `战斗器.html` + `战斗器.js` | 1711 | 战斗计算器。消费建卡器导出的 JSON |
| `鉴定器.html` + `鉴定器.js` | 234 | 1D2 鉴定投掷器 |
| `建卡器.css` | 454 | 三页共用 |
| `图片/*.png` | — | 七罪图标，文件名是中文罪孽名 |

数据流：`建卡器 --导出 JSON--> 战斗器`；`建卡器 --localStorage("limbus-trpg-character")--> 鉴定器`。

---

## 五条硬性约定（违反会出真问题）

**1. 战斗器绝不解析中文卡面文本。** 所有数字都由建卡器以结构化字段导出。要让战斗器认识一个新效果，就去建卡器给它加 `stats`，不要在战斗器里写正则。唯一的例外是多重攻击追加卡的基础伤害（`战斗器.js` 里一处 `/基础伤害\s*(\d+)/`）。

**2. 改了 CSS 或任何 JS，必须把三个 HTML 里的 `?v=N` 一起 +1。** 否则浏览器拿缓存的旧版，会浪费大量时间排查"改了没生效"。当前是 `?v=25`。
```bash
sed -i 's/?v=25"/?v=26"/g' index.html 战斗器.html 鉴定器.html
```

**3. `建卡器.css` 是三页共用的。** `.gender-row` / `.attr-help` / `.field` / `.sec-head` / `.btn` / `.card` 等被建卡器和鉴定器使用，**不要就地改**。战斗器专用样式一律加在文件末尾 `/* ====== 战斗计算器 ====== */` 段内，用只在战斗器出现的类名。

**4. `checkOdds()` 在 `建卡器.js:70` 和 `鉴定器.js:35` 各有一份，改动要同步。** 两处注释都写明了。

**5. 导出的 JSON 不含任何规则文本。** 它只是战斗器的输入，不是给人读的规则书。`buildData()` 里刻意剥掉了速查表和每张卡重复的拼点公式（`withoutClash()`）。加字段前先想清楚战斗器是否真的要读。

---

## stats схема（最重要的部分）

权威定义写在 `建卡器.js` 的 `SIN_TRAIT_QA`（第 315 行）上方注释里，**改动前先读那段**。摘要：

- `CARD_BASE_STATS`（第 208 行）：非攻击卡的**基础效果**，按 `trait × level` 索引，29 条已全覆盖。攻击类用 `baseDamage` / `hits`。
- `SIN_TRAIT_QA` 每个选项上的 `stats`：**问答特效**，249 项中已结构化 162（其中 26 条 `partial`）。

字段分类：

| 类别 | 字段 |
|---|---|
| 本轮持续 | `intentDown` `dmgTakenUp` `dmgDealtDown` `dmgTakenDown` `diceUp` |
| 即时 | `damage`（配 `onHit` 限定命中才触发）`heal` `temp` `pressure` |
| 恒作用于打出者 | `selfDamage` `selfPressure` `selfHeal` `selfTemp`（不受 scope 影响） |
| 只作用于本次 | `thisDice` `thisDamage` `thisIntentDown`（投骰前折进拼点，不留到本轮） |
| 接线卡胜负分支 | `win:{...}` / `lose:{...}`，形状同 `CARD_BASE_STATS` |
| 打断/驱散 | `cancel:{kind:"buff"\|"debuff"\|"any", n:数量\|"all"}` + `orElse:{...}` 替代条款 |
| 标记 | `partial:true` 表示只自动了一部分，界面会显示「部分自动，余下手动」 |

作用面 `scope`：`target` `adjOne` `adjAll` `otherAll` `allFoes` `self` `guarded`（被庇护的友方）`oneAlly` `selfAndAlly` `allAllies`。
`adjOne` / `oneAlly` / `selfAndAlly` 需要玩家二次指定对象，战斗器会自动补选择器并拦住打出按钮。

**「相邻」= 敌人列表里的上下相邻**，站位就是数组顺序，战斗器里可用 ↑↓ 调整。

### 什么不挂 stats（87 条，故意的）
条件判断（「若你本轮已受到过伤害」）、延时伤害（灼烧）、元效果（「本卡所有恢复 +2」）、替换语义（「改为获得 6 点」而非叠加）、【切换】类。这些界面上标「需自行结算」，交给 GM。**不要为了凑覆盖率硬做**——半对的自动化比不自动更糟。

---

## 战斗器的关键结构

- `CARD_KIND`（第 106 行）把 trait 分五类，决定出牌路径：
  `attack`（选敌人+意图→投骰）· `reaction`（接线）· `ally`（选友方）· `foe`（选敌人压意图值）· `self`（选牌弃掉）
- **接线不占行动槽**，每轮限一次，在自己的行动里主动打出。防御/反击接谁的都行，援护只能替别人挡。
- **没有「敌方回合」这个阶段**。所有人行动完、仍有攻击意图没人接线时，`#foeCard` 面板自动出现结算剩余攻击。
- `shotsOf()`（第 810 行）：多重攻击每一击独立选目标与意图，单击是只有一击的特例。
- 阵亡处理在一步操作**全部结算完之后**统一清场：敌人移除、我方 0 血退场但保留在名单上（改 HP 可复归）。
- 敌方攻击伤害是 `XdY+Z`，结算时现掷。

### 规则书没写、本实现采用的口径
接线失败时伤害 = `敌方基础伤害 + (意图值 − 你的拼点值)`，与「未接线」公式同形（未接线相当于不投骰、拼点值退化为体魄）。写在 `战斗器.js` 文件头注释里。

---

## 改题库文案时

`建卡器.js:2867` 的 `DEPRECATED_OPTIONS` 是存档迁移表。改了某个问答选项的含义后，把 `{sin, trait, level, q, o}` 加进去，旧存档读档时会清空那一问并提示玩家重选。已有 100+ 条，是这个项目一直在守的规矩。

---

## 怎么测

**起服务**（`.claude/launch.json` 已配好）：用 `preview_start` 传 `{name:"limbus-trpg"}`，python 起在 8777。然后 `navigate` 到 `http://localhost:8777/战斗器.html?cb=N`（`cb` 随便换个数破缓存）。

**驱动战斗器**：用 `javascript_tool` 直接注入角色。战斗器不读 localStorage，`playerFromJson(obj)` 是入口：
```js
const p = playerFromJson({基本信息:{名字:"甲"}, 基础属性:{...}, 衍生数值:{最大生命值:24},
  sins:{sinPressure:0,sinPressureCap:4}, 攻击模式:{A组:{...}}, 罪孽卡片:{a:{...},b:{}}});
state.players.push(p); state.enemies.push(newEnemy()); renderAll();
```
然后点 `[data-spot]` → `[data-card]` → `[data-foe]` → 填 `#manualRoll` → 点 `#btnGo`，读 `state.log[0].lines` 核对。**手填骰值让结果可复现**。

**无头跑建卡器**（校验 stats、生成导出）：`node .claude/headless.js <脚本.js>`，它用 DOM 桩加载 `建卡器.js` 后执行你的脚本，`state` / `buildData()` / `getSinQA()` 全都可用。

**语法检查**：`node --check 建卡器.js && node --check 战斗器.js && node --check 鉴定器.js`

---

## 已知空白

- **罪孽压力鉴定**没做：卡片会自动加减压力，但压力满不会触发意志鉴定（难度=本场第几次满，通过则清零但上限-1，失败混乱一回合）。
- **E.G.O 发动**没做：建卡器能建 E.G.O 并导出，战斗器完全不认。
- **我方状态槽**没有：敌人有增益/减益意图槽可被驱散，我方没有对应结构，所以「移除一个减益效果」这类卡（如色欲·涤净）只能手动。
- 「灵活——看到第一击结果再决定第二击目标」需要分步结算，当前是两击目标一次选完再一起投骰。

## 风格

注释解释**为什么**（配平意图、规则出处、跨文件耦合点），不复述代码在做什么。现有代码密度较高，跟着写。用户是规则作者，规则判断以他为准——拿不准的口径直接问，不要静默发明。
