# Limbus Company TRPG 工具集

纯前端、无构建、无依赖。四个页面共用一份 CSS 与 localStorage。中文注释、中文文件名。

| 文件 | 行数 | 职责 |
|---|---|---|
| `index.html` + `建卡器.js` | 3160 | 8 步车卡向导。**全部规则数据的唯一来源** |
| `战斗器.html` + `战斗器.js` | 1760 | 战斗计算器。消费建卡器导出的 JSON |
| `题库.html` + `题库.js` | 160 | 只读速查：全部卡片与问答平铺一页。**直接引用 `建卡器.js` 当数据源** |
| `鉴定器.html` + `鉴定器.js` | 234 | 1D2 鉴定投掷器 |
| `建卡器.css` | 500 | 四页共用 |
| `图片/*.png` | — | 七罪图标，文件名是中文罪孽名 |

数据流：`建卡器 --导出 JSON--> 战斗器`；`建卡器 --localStorage("limbus-trpg-character")--> 鉴定器`。

---

## 五条硬性约定（违反会出真问题）

**1. 战斗器绝不解析中文卡面文本。** 所有数字都由建卡器以结构化字段导出。要让战斗器认识一个新效果，就去建卡器给它加 `stats`，不要在战斗器里写正则。唯一的例外是多重攻击追加卡的基础伤害（`战斗器.js` 里一处 `/基础伤害\s*(\d+)/`）。

**2. 改了 CSS 或任何 JS，必须把四个 HTML 里的 `?v=N` 一起 +1。** 否则浏览器拿缓存的旧版，会浪费大量时间排查"改了没生效"。当前是 `?v=48`。
```bash
sed -i 's/?v=48"/?v=49"/g' index.html 战斗器.html 鉴定器.html 题库.html
```

**3. `建卡器.css` 是四页共用的。** `.gender-row` / `.attr-help` / `.field` / `.sec-head` / `.btn` / `.card` 等被建卡器和鉴定器使用，**不要就地改**。单页专用样式一律加在文件末尾自己那一段里（`/* ====== 战斗计算器 ====== */`、`/* ====== 题库速查 ====== */`），类名只在该页出现（战斗器用 `.ptag` 等，题库统一 `.qa-` 前缀）。

**4. `checkOdds()` 在 `建卡器.js:70` 和 `鉴定器.js:35` 各有一份，改动要同步。** 两处注释都写明了。

**5. 导出的 JSON 不含任何规则文本。** 它只是战斗器的输入，不是给人读的规则书。`buildData()` 里刻意剥掉了速查表和每张卡重复的拼点公式（`withoutClash()`）。加字段前先想清楚战斗器是否真的要读。

---

## stats схема（最重要的部分）

权威定义写在 `建卡器.js` 的 `SIN_TRAIT_QA`（第 315 行）上方注释里，**改动前先读那段**。摘要：

- `CARD_BASE_STATS`（第 208 行）：非攻击卡的**基础效果**，按 `trait × level` 索引，29 条已全覆盖。攻击类用 `baseDamage` / `hits`。
- `SIN_TRAIT_QA` 每个选项上的 `stats`：**问答特效**。覆盖率**不要在这里手写**——会过时；打开 `题库.html`，顶部统计条是现算的（四档分别是：打出时自动 · 部分自动 · 需自行结算 · 【切换】切进时自动）。

字段分类：

| 类别 | 字段 |
|---|---|
| 本轮持续 | `intentDown` `dmgTakenUp` `dmgDealtDown` `dmgTakenDown` `diceUp` |
| 即时 | `damage`（配 `onHit` 限定命中才触发）`heal` `temp` `pressure` |
| 恒作用于打出者 | `selfDamage` `selfPressure` `selfHeal` `selfTemp`（不受 scope 影响） |
| 只作用于本次 | `thisDice` `thisDamage` `thisIntentDown`（投骰前折进拼点，不留到本轮）· `fixedRoll:N`（不投骰，骰值固定为 N；优先于手填与随机） |
| 接线卡胜负分支 | `win:{...}` / `lose:{...}`，形状同 `CARD_BASE_STATS`；另有 `freeSwitch:true`（免费切架势，不占行动槽）· `tempPerMargin:N`（差值×N 转临时生命）· `blockAll:true`（挡下该敌人全部攻击意图，按敌人类型递减） |
| 打断/驱散 | `cancel:{kind:"buff"\|"debuff"\|"any", n:数量\|"all"}` + `orElse:{...}` 替代条款 |
| 我方状态槽 | `cleanse:N`（移除 N 个减益）· `steal:N`（把减益搬到敌人身上，落成一条减益意图） |
| 敌我两边 | `allyScope` + `allyTemp` / `allyHeal` / `allyDiceUp`——一条特效打敌人的同时惠及友方，友方那半的作用面单独写 |
| 弃牌 | `discardKind:"attack"\|"reaction"`（限定基础弃牌的种类，**不改张数**）· `extraDiscard:N`（额外弃 N 张，给没有弃牌步骤的卡用）· `needDiscard:true`（弃牌是发动条件，凑不够张数时整条不发动且一张也不弃） |
| 条件 | `cond:{...}`——「若…」的机器可读版。拼点前能算的（`selfHurt` `selfHpBelowHalf` `selfHpAboveFoe` `intentAboveAttr`…）门控 `this*`；拼点后才知道的（`hit` `miss` `killed` `firstHit` `allHit`…）门控普通效果。求值器拿不到上下文时一律返回 false，不猜 |
| 替换 | `altIf:{cond,...}`（条件成立时整个盖掉，而不是叠加）· `replaceWin/replaceLose`（换掉接线卡的基础胜负分支）· `sameModeAlt`（两组攻击模式相同时的替代值） |
| 元效果 | `healMod:±N`（本卡所有恢复与临时生命）· `roundHealMod:±N`（本轮，跨卡） |
| 运行时取值 | `damagePerTemp` `damagePerBlocked` `healPerDamage`（按临时生命/挡下伤害/造成伤害的比例算） |
| 分击 | `shot:N` + `shotDice` / `shotDamage`（可为负）——只作用于多重攻击的第 N 击；`altAttr` 改用另一组模式的拼点属性 · `canClash:true` 解开该击的拼点（傲慢「灵活」）· `extraHits:N` 加击数（傲慢「连击」，只对 `hits>1` 的卡生效） |
| 切换 | `onSwitchOut:{...}`——搭在 `thenSwitch` 上的「切出去时」条款（倾泻/久眠），与切进来的 `onSwitchIn` 是两回事 |
| 卡组 | `devour:N`（弃光本组按张数回血并刷新）· `restore:N`（从 `g.used` 这个弃牌堆取回 N 张） |
| 标记 | `partial:true` 表示只自动了一部分，界面会显示「部分自动，余下手动」· `noop:true` 描述的是战斗器默认行为 |

**`onSwitchIn:true` 标在选项对象上，不在 `stats` 里。** 15 条「切换到此攻击模式时…」的触发点是切进这一组、不是打出这张卡，放进 `stats` 会被 `qaStats()` 在打出时误结算。战斗器用 `switchInEffects()` 在切换时把它们列出来提醒 GM，仍不自动结算。怠惰的「【切换】倾泻 / 久眠」是搭在 `thenSwitch` 上的「切出去时」条款，不属于这一类。

**`excludes:[{q,o}]` 也标在选项对象上，不在 `stats` 里。** 同一张卡内的互斥对（目前只有傲慢「连击」↔「灵活」：三击 + 两次拼点两头都占）。**只在一边声明**，求值时对称展开——两边都写必然会改一边忘另一边。三处消费它：建卡器 `excludedOptions()` 把冲突项锁灰并拦住点击、`migrateDeprecatedCardAnswers()` 读档时清掉靠后那一问（互斥是后加的规则，早于它的存档两边都可能选了）、题库两侧各标一个「与「X」互斥」。战斗器不认这个字段——建卡器根本产不出冲突的导出。

作用面 `scope`：`target` `adjOne` `adjAll` `otherAll` `allFoes` `self` `guarded`（被庇护的友方）`oneAlly` `selfAndAlly` `allAllies`。
`adjOne` / `oneAlly` / `selfAndAlly` 需要玩家二次指定对象，战斗器会自动补选择器并拦住打出按钮。

**「相邻」= 敌人列表里的上下相邻**，站位就是数组顺序，战斗器里可用 ↑↓ 调整。

### 什么不挂 stats（故意的）
条件判断（「若你本轮已受到过伤害」）、延时伤害（灼烧）、元效果（「本卡所有恢复 +2」）、替换语义（「改为获得 6 点」而非叠加）、【切换】类。这些界面上标「需自行结算」，交给 GM。**不要为了凑覆盖率硬做**——半对的自动化比不自动更糟。

**【切换】那 15 条现在也挂 stats 并自动结算**（`setStance()` → `switchInLines()`）。要指定单个对象的取敌人列表第一个，日志写明是谁。`onSwitchIn` 仍标在选项对象上而不是 stats 里，且 **`qaStats()` 必须把它们排除**——否则打出那张卡时会被误结算一次。

---

## 战斗器的关键结构

- `CARD_KIND`（第 106 行）把 trait 分五类，决定出牌路径：
  `attack`（选敌人+意图→投骰）· `reaction`（接线）· `ally`（选友方）· `foe`（选敌人压意图值）· `self`（选牌弃掉）
- **接线占一个行动槽**，每轮限一次，在自己的行动里主动打出。防御/反击接谁的都行，援护只能替别人挡。防御就是这一槽的行动——防完不能再出牌，单槽角色因此是「攻」与「防」二选一。接线卡仍可跨组打出（不限当前槽位那一组），但照样吃掉这一个槽。
- **没有「敌方回合」这个阶段**。所有人行动完、仍有攻击意图没人接线时，`#foeCard` 面板自动出现结算剩余攻击。这一步没有接线分支——走到这里所有人的槽位必然已空，防御必须提前打出。
- **架势 = 角色当前用哪一组卡**（`p.stance`，`"a"`/`"b"`），所有槽位都从这一组出牌——没有「槽 1 用 A 组、槽 2 用 B 组」这回事。三条切换路径都走 `setStance()`：聚光灯里的主动切换**占一个行动槽**；卡面自带的 `thenSwitch` 与 `win.freeSwitch` 不占；角色卡上的 A/B 开关免费且不触发【切换】特效，定位是开局设定与 GM 纠正。
- **弃牌一律玩家自选，没有随机弃牌。** 两个来源：特殊卡基础的 `stats.discard`，与问答的 `extraDiscard`。问答的 `discardKind` 只过滤选择器里列哪些牌，不加张数。都从**本卡所在那一组**的未使用牌里挑（本卡自己正在打出，不算），收益是**叠加**的：基础恢复 + 问答恢复。
  **弃牌是发动条件，全有或全无**：可弃的牌凑不够要求张数时，特殊卡的基础恢复与所有 `needDiscard` 的问答条目整条不发动，**且一张也不弃**（不能白付一半代价）；同卡的伤害与不依赖弃牌的条目（如「饥饿感」）照常结算。所以 `discardNeed()` 在凑不够时返回 0 而不是 `pool.length`——它既是界面要玩家点几张，也保证流程不会卡死。`playCard()` 开头就算出 `met` 传给 `applyAllQa()`，因为攻击分支要在真正消耗弃牌之前结算问答。
- **傲慢不吃差值伤害**：`SIN_NO_MARGIN_DAMAGE`（建卡器）决定哪些罪孽的攻击卡带 `noMarginDamage`，按**卡**导出——战斗器不该知道「傲慢」两个字意味着什么。伤害 = 基础 + 卡面/模式调整，差值那份拿不到，单方面攻击也一样。作为交换它的拼点最强（`fixedRoll` 不投骰 + 精准 +2/+3），基础伤害已按"把差值补回来"上调（4/6/9，多重攻击每击 5）。**只管伤害**：傲慢·防御的 `tempPerMargin` 照常吃差值。
- **敌人分三档 `kind`**（`FOE_KINDS`）：`mob` 杂兵 · `elite` 精英 · `boss`。**它只影响一条规则**——`blockAll` 对它生效到什么程度（全挡 / 只挡打向同一目标的 / 免疫）。HP、意图值、伤害都不受类型影响，那些本来就是 GM 手填的。
- **防御为什么要给胜利收益**：攻击拼过敌方意图就消掉它（`playCard` 攻击分支里 `intent.resolved = true` 是无条件的，拼输也算），等于自带一次**确定**的格挡还附带伤害；防御原本胜利只有 `block`，于是被攻击完全支配。现在胜利侧补了 `heal` 与 `tempPerMargin`，并把「挡下全部意图」放进两条大技能问答（傲慢「固若金汤」、暴食「囫囵吞下」）——攻击一次只消一条意图，防御能消一片，这是防御唯一压得过攻击的杠杆。
- `shotsOf()`：多重攻击每一击独立选目标，单击是只有一击的特例。**但默认只有第一击拼点**——后面几击不投骰、自动命中，也因此**消不掉敌方意图**。要解开第二次拼点必须选出带 `canClash` 的问答（傲慢「灵活」），`canClashShot(card, n)` 是唯一的判定入口，出牌界面、意图自动指派、结算三处都走它。这条是多重攻击的定价：一张牌消两条攻击意图会压过所有单击攻击卡。
  连带影响两条问答：`altAttr`（「变招」）在不拼点的那一击上没有落点，`shotMods()` 会退到 `sameModeAlt`；`shotDice` 同理被清零（所以「不懈」给的是伤害不是加骰）。
- 阵亡处理在一步操作**全部结算完之后**统一清场：敌人移除、我方 0 血退场但保留在名单上（改 HP 可复归）。
- 敌方攻击伤害是 `XdY+Z`，结算时现掷。

### 我方状态槽
`p.effects = [newEffect()]`：每个状态**自带数值**（`dice` 拼点骰 ±N、`dmgDown` 减伤 N），`clashParts()` 与 `applyDamage()` 会自动汇总——所以移除一个状态就真的把数值拿掉了，别再往「拼点修正」重复填。`lasting:false` 的在 `nextRound()` 清掉，`lasting:true` 的留着等人来解（否则「涤净」在回合末尾打就没东西可涤）。状态由 GM 在角色卡 ⚙调整 里手动加；`cleanse` / `steal` 两个 stats 字段消费它。

### 规则书没写、本实现采用的口径
接线失败时伤害 = `敌方基础伤害 + (意图值 − 你的拼点值)`，与「未接线」公式同形（未接线相当于不投骰、拼点值退化为体魄）。写在 `战斗器.js` 文件头注释里。

---

## 题库速查页

`题库.html` 把七罪的卡片基础效果与全部问答平铺成一页，带筛选与搜索，只读。**它不复制任何规则文案**——直接 `<script src="建卡器.js">` 读 `SIN_TRAIT_QA` / `CARD_BASE_STATS` 等表（经典脚本的顶层 `const` 在全局词法环境里，同页其他脚本可以按名字读到）。

这靠一条约定成立：**`建卡器.js` 末尾的事件绑定与初始化整体包在 `if(stageCard){…}` 里**。题库页没有 `#stageCard`，于是它退化成纯数据模块——不绑事件、不读写 localStorage、不渲染。往那个 `if` 外面加带副作用的顶层代码会破坏这一点。（`.claude/headless.js` 的 DOM 桩里 `stageCard` 恒为真值，所以无头跑法不受影响。）

`题库.js` 整个包在 IIFE 里：`建卡器.js` 有 150+ 个顶层声明（`state` / `render` / `toast` …），不隔离必然撞名。

自动化四档的判定必须和战斗器一致，顺序是 `onSwitchIn` → 无 `stats` → `partial` → 其余；`onSwitchIn` 的条目 `stats` 是 `null`，先判它，否则会被误标成「需自行结算」。

---

## 改题库文案时

`建卡器.js:2867` 的 `DEPRECATED_OPTIONS` 是存档迁移表。改了某个问答选项的含义后，把 `{sin, trait, level, q, o}` 加进去，旧存档读档时会清空那一问并提示玩家重选。已有 100+ 条，是这个项目一直在守的规矩。

---

## 改了战斗规则，另外两页也要跟

**规则书正文写在建卡器的向导里**，不在战斗器里。改了战斗器的行为，这三处极容易忘：

- `renderStep2()`（`建卡器.js:1622`，「战斗规则」那一大块 `.attr-help`）——接线是否占槽、差值怎么算、未接线的公式都写在这里。这一段和战斗器不一致，玩家看到的规则就是错的。
- `renderCardOverview()`（`建卡器.js:2158`）——切换架势的代价、弃牌口径。
- `题库.js` 的 `autoKindOf()` / `baseTag()` 必须镜像战斗器的判定。`baseTag()` 要**按 trait 判**：`attack` / `multiAttack` 三个等级都自动（它们导出的是 `baseDamage` / `hits`，不进 `CARD_BASE_STATS`），按等级判会把小/大技能的攻击卡误标成「需自行结算」。

反过来，**导出格式不用同步**：`buildCardGroupExport()` 用 `stats: o.stats || null` 整体透传，`stats` 里加字段不需要动建卡器；只有加**卡片级**字段（像 `noMarginDamage`、`hits`）才要改那个函数。

---

## 怎么测

**起服务**（`.claude/launch.json` 已配好）：用 `preview_start` 传 `{name:"limbus-trpg"}`，python 起在 8777。然后 `navigate` 到 `http://localhost:8777/战斗器.html?cb=N`（`cb` 随便换个数破缓存）。

**驱动战斗器**：用 `javascript_tool` 直接注入角色。战斗器不读 localStorage，`playerFromJson(obj)` 是入口：
```js
const p = playerFromJson({基本信息:{名字:"甲"}, 基础属性:{...}, 衍生数值:{最大生命值:24},
  sins:{sinPressure:0,sinPressureCap:4}, 攻击模式:{A组:{...}}, 罪孽卡片:{a:{...},b:{}}});
state.players.push(p); state.enemies.push(newEnemy()); renderAll();
```
然后点 `[data-spot]` → `[data-card]` → `[data-foe]` → 填 `#manualRoll` → 点 `#btnGo`，读 `state.log[0].lines` 核对。**手填骰值让结果可复现**。

**无头跑建卡器**（校验 stats、生成导出）：`node .claude/headless.js <脚本.js>`，它用 DOM 桩加载 `建卡器.js` 后执行你的脚本，`state` / `buildData()` / `getSinQA()` 全都可用。

**语法检查**：`node --check 建卡器.js && node --check 战斗器.js && node --check 鉴定器.js && node --check 题库.js`

---

## 已知空白

- **罪孽压力鉴定**没做：卡片会自动加减压力，但压力满不会触发意志鉴定（难度=本场第几次满，通过则清零但上限-1，失败混乱一回合）。
- **E.G.O 发动**没做：建卡器能建 E.G.O 并导出，战斗器完全不认。E.G.O 里那几条「所有敌人本轮受到的伤害 +N」（燃尽 / 余烬 / 衰弱）要靠敌人 ⚙调整 里的手填格子落地。
- **本轮伤害修正只有卡片能自动加，其余靠手填**：敌人 ⚙调整 有「本轮受到伤害 +」「本轮造成伤害 -」，我方 ⚙调整 有「本轮受伤 -」。它们和自动来源共用同一个字段（`roundDmgTaken` / `roundDmgDealt` / `roundDmgDown`），所以卡片已经算过的不要重复填；三项都在 `nextRound()` 清零。
- 249 个问答选项**全部自动结算**（234 打出时 + 15 切进时）。原先唯一手动的「灵活」已改成「第二击也能拼点」，不再需要分步结算。

## 风格

注释解释**为什么**（配平意图、规则出处、跨文件耦合点），不复述代码在做什么。现有代码密度较高，跟着写。用户是规则作者，规则判断以他为准——拿不准的口径直接问，不要静默发明。
